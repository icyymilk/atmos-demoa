import { body,checkOrigin,fail,owner } from '@/lib/storage';
import { harnessRequest } from '@/lib/harness-client';
export async function GET(request:Request){try{const user=await owner(request);const response=await harnessRequest('/catalog',{owner:user},request.signal);return new Response(response.body,{status:response.status,headers:{'Content-Type':'application/json','Cache-Control':'no-store'}});}catch(error){return fail(error);}}
export async function POST(request:Request){try{checkOrigin(request);const user=await owner(request);const input=await body(request,2000);const response=await harnessRequest('/plugins',{id:input.id,enabled:input.enabled,owner:user},request.signal);return new Response(response.body,{status:response.status,headers:{'Content-Type':'application/json'}});}catch(error){return fail(error);}}
